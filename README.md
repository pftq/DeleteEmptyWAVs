Handy tool for deleting empty wav files in the current folder. Useful when generating stems from something like FL Studio where it'll include channels with no sound.

To use, download the EXE and put it in the same folder as the WAVs.  Then run it.
